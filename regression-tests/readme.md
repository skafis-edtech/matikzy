# Regression tests

Tests if matikzy generates the same tikz for a set of problems. That is, after some small change or addition testing to see if the rest of the functions work the same (display is the same).

```bash
node --test
```
